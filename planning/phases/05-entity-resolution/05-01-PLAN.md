---
phase: 05-entity-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  src/services/similarity_service.py,
  src/services/__init__.py
]
autonomous: true
requirements: [KR-04]
user_setup: []

must_haves:
  truths:
    - "Entity name pairs produce Jaro-Winkler similarity scores between 0.0 and 1.0"
    - "Entity description pairs produce embedding similarity scores between 0.0 and 1.0"
    - "Combined similarity score uses weighted formula: 0.4 × string + 0.6 × embedding"
    - "Service handles null/empty entity fields gracefully"
  artifacts:
    - path: "src/services/similarity_service.py"
      provides: "Entity similarity computation with Jaro-Winkler and embedding"
      exports: ["compute_similarity", "compute_jaro_winkler", "compute_embedding_similarity"]
    - path: "src/services/__init__.py"
      provides: "Package export for similarity service"
      exports: ["SimilarityService"]
  key_links:
    - from: "src/services/similarity_service.py"
      to: "src/services/embedding_service.py"
      via: "import EmbeddingService"
      pattern: "from .embedding_service import EmbeddingService"
    - from: "src/services/similarity_service.py"
      to: "src/utils/text_utils.py"
      via: "import text normalization functions"
      pattern: "from ..utils.text_utils import normalize_entity_name"

<objective>
Create similarity computation service that computes Jaro-Winkler and embedding similarities for entity deduplication with ≥85% accuracy threshold.

Purpose: Enable accurate detection of duplicate entities by combining string and semantic similarity measures
Output: Similarity service with methods for computing Jaro-Winkler distance, embedding cosine similarity, and weighted hybrid score
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-entity-resolution/05-CONTEXT.md
</context>

<interfaces>
<!-- Extracted from existing codebase patterns -->
From src/services/embedding_service.py (expected):
```python
class EmbeddingService:
    def compute_embedding(self, text: str) -> List[float]:
        """Compute embedding vector for text"""
        pass
    
    def cosine_similarity(self, vec1: List[float], vec2: List[float]) -> float:
        """Compute cosine similarity between two vectors"""
        pass
```

From src/models/entity.py (expected):
```python
class Entity:
    def __init__(self, id: str, name: str, type: str, description: str, 
                 created_at: datetime, updated_at: datetime):
        self.id = id
        self.name = name
        self.type = type
        self.description = description
        self.created_at = created_at
        self.updated_at = updated_at
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create similarity service interface and Jaro-Winkler implementation</name>
  <files>src/services/similarity_service.py</files>
  <action>
    Create SimilarityService class with:
    - Constructor that initializes any required dependencies
    - compute_jaro_winkler(str1, str2) method implementing Jaro-Winkler distance algorithm
    - Proper handling of edge cases (empty strings, null values, different lengths)
    - Normalization of scores to 0.0-1.0 range where 1.0 is identical
    - Use of existing text normalization utilities for entity names
    - Logging for similarity computation debugging
    Per D-18: Use Jaro-Winkler distance for string similarity (handles typos, spelling variations well)
  </action>
  <verify>
    <automated>python -m pytest tests/services/test_similarity_service.py::TestSimilarityService::test_jaro_winkler_identical -v</automated>
  </verify>
  <done>
    Jaro-Winkler method returns 1.0 for identical strings, 0.0 for completely different strings, and correct intermediate values for known test cases
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement embedding similarity and hybrid scoring</name>
  <files>src/services/similarity_service.py, tests/services/test_similarity_service.py</files>
  <behavior>
    - Test 1: compute_embedding_similarity returns cosine similarity between two embedding vectors
    - Test 2: compute_similarity combines Jaro-Winkler and embedding similarities using 0.4/0.6 weights
    - Test 3: handle_entity_pair computes similarity for two Entity objects
    - Test 4: service handles null/empty description fields by using name-only similarity
  </behavior>
  <action>
    Implement in similarity_service.py:
    - compute_embedding_similarity(emb1, emb2) method using EmbeddingService
    - compute_similarity(name_sim, emb_sim) method applying 0.4×string + 0.6×embedding formula
    - handle_entity_pair(entity1, entity2) method that:
      * Normalizes entity names
      * Computes Jaro-Winkler similarity on names
      * Computes embedding similarity on descriptions (or names if descriptions missing)
      * Returns weighted hybrid score
    - Dependency injection for EmbeddingService in constructor
    - Proper error handling for embedding computation failures
    Per D-17, D-18, D-19, D-20, D-21: Hybrid approach with Jaro-Winkler and embedding similarity, normalized to 0-1, threshold ≥0.85
  </action>
  <verify>
    <automated>python -m pytest tests/services/test_similarity_service.py -v</automated>
  </verify>
  <done>
    Similarity service correctly computes hybrid scores for entity pairs, handles missing data, and produces scores in 0.0-1.0 range
  </done>
</task>

</tasks>

<verification>
Verify similarity service integrates with embedding service and produces correct similarity distributions for known entity pairs
</verification>

<success_criteria>
Similarity service computes Jaro-Winkler and embedding similarities correctly, combines them with proper weighting, and handles edge cases without crashing
</success_criteria>

<output>
After completion, create .planning/phases/05-entity-resolution/05-01-SUMMARY.md
</output>