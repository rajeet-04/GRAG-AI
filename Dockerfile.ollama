FROM ollama/ollama:latest

# Set Ollama host to allow external connections
ENV OLLAMA_HOST=0.0.0.0:11434

# Run Ollama service; model pulls/warmups are handled by compose init stages.
ENTRYPOINT ["/bin/sh", "-c"]
CMD ["ollama serve"]
